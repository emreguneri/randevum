# Shared Package

Shared types, DTOs, constants, validators, and utilities consumed by mobile apps, admin panel, and API. All monetary constants are expressed in integer kuruş.

TODO: publish as workspace package and import across apps/services.

